void setup() {
  analogWriteFreq(25000);
  
}

void loop() {
  // put your main code here, to run repeatedly:
  int potVal = analogRead(A0);
  int myFan = map(potVal, 0, 4095, 0, 255);
  int myKey = map(potVal, 0, 4095, 0, 255);
  analogWrite(D1, myFan);
  analogWrite(D0, myKey);
}
