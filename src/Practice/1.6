const int POTENTIOMETER_PIN = 26;

void setup() {
  pinMode(D0, OUTPUT);
  
  adc_init();
  adc_gpio_init(POTENTIOMETER_PIN);
  adc_select_input(D0);
  
  pwm_gpio_init(D0);
  uint slice_num = pwm_gpio_to_slice_num(D0);
  pwm_set_wrap(slice_num, 255);
  pwm_set_enabled(slice_num, true);
}

void loop() {
  uint16_t pot_value = adc_read();
  
  pwm_set_chan_level(slice_num, PWM_CHAN_A, pot_value);
  
  sleep_ms(8);
}

//разобраца,че дакак
